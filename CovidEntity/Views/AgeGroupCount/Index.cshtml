@model IEnumerable<CovidEntity.Models.AgeGroupCount>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CovidCount.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartAge)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EndAge)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AgeCount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CovidCount.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StartAge)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EndAge)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AgeCount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
